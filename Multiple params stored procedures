#Database in use
use larsql;

DELIMITER :

CREATE PROCEDURE ShowProductsFromDB(in maxId int, in title varchar(50))
begin
	select * from products where id < maxId && name = title;
end:


CALL ShowProductsFromDB(9, 'The 39 Steps');
